server:
  port: 8080
  servlet:
      context-path: /bootcamp

spring:
  datasource:
    driver-clase-name: org.postgresql.Driver
    url:  jdbc:postgresql://localhost:3333/postgres
    username: postgres
    password: 123456789
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: none
      

application:
  description: Projeto criado para o bootcamp DIO;